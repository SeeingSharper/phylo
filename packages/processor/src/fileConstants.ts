/**
 * Constants shared between file-based readers and writers
 */

/**
 * Prefix for file-based inputs
 * File inputs have names like "file:/path/to/file.md"
 */
export const FILE_INPUT_PREFIX = 'file:';

